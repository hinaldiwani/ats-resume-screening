/**
 * static/js/api.js
 *
 * Central place for frontend API calls. Currently a placeholder — actual
 * fetch calls to /api/v1/* endpoints will be added once those endpoints
 * have real logic.
 */

const API_BASE_URL = "/api/v1";

// Example wrapper to be implemented later:
// async function apiRequest(path, options = {}) { ... }
